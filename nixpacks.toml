[phases.setup]
nixPkgs = ["python313"]

[phases.install]
cmds = ["python -m pip install --break-system-packages -r backend/requirements.txt"]

[phases.build]
cmds = ["echo 'no build step'"]

[start]
cmd = "python -m uvicorn backend.app.main:app --host 0.0.0.0 --port $PORT"
